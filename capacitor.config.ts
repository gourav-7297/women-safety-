import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.securezonenavi.app',
  appName: 'secure-zone-navi',
  webDir: 'dist'
};

export default config;
